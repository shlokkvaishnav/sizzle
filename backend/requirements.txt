# --- Petpooja AI Copilot -- Backend Dependencies ---
# All local, no external API SDKs needed

# Web framework
fastapi
uvicorn[standard]
python-multipart

# Database (Supabase PostgreSQL)
sqlalchemy==2.0.36
psycopg2-binary>=2.9.10

# Data processing
pandas
numpy

# Association rule mining (combo engine - Person D)
mlxtend

# Voice -- local STT (runs Whisper on your CPU, no API)
faster-whisper

# Voice Activity Detection -- filters noise before Whisper (fully local)
torch

# Fuzzy text matching (Person B - item matching)
rapidfuzz

# Semantic vector matching — hybrid fuzzy+semantic for accent robustness
# paraphrase-multilingual-MiniLM-L12-v2 model downloads ~420MB on first run
sentence-transformers>=2.7.0
faiss-cpu>=1.8.0

# TTS — Edge TTS (Microsoft neural voices, supports Indian languages)
# No model downloads needed — uses Microsoft Edge's TTS service
edge-tts>=7.0.0

# TTS text normalization — script conversion for Indian languages
indic-transliteration>=2.3.0

# LLM integration — async HTTP client for Ollama
httpx>=0.27.0

# Session persistence — Redis preferred, DB fallback built-in
redis>=5.0.0

# JWT authentication
PyJWT>=2.8.0

# Utilities
python-dotenv

